#include<bits/stdc++.h>
#define fast() ios_base::sync_with_stdio(false);cin.tie(nullptr);
#define test() int t; cin >> t; while(t--)
#define ll long long

using namespace std;

int32_t main(){

  fast();
  test();
  string a, b; cin >> a >> b;

      swap(a[0],b[0]);
      cout << a << " " << b <<'\n';
  
  return 0;
}
